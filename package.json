{
  "name": "echo",
  "version": "0.0.0",
  "private": true,
  "description": "ECHO independent blockchain protocol implementation.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --test dist-tests/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
